import { userSchema } from "./user";
import { messagesSchema } from "./messages";
import { postSchema } from "./post";

const schemas = [userSchema, messagesSchema, postSchema];

export default schemas;
