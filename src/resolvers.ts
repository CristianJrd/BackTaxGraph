// import { userResolver } from "./user";
import { messagesResolver } from "./messages";
import { postResolver } from "./post";
const resolvers = [/* userResolver, */ messagesResolver, postResolver];

export default resolvers;
